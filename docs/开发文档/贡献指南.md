# 贡献指南

感谢你有兴趣为 1611 值日系统做出贡献！本文档将指导你如何参与项目开发。

---

## 🤝 如何参与

### 方式一：成为协作者（Collaborator）

**适合**：项目核心成员、信任的团队成员

如果你是项目维护者邀请的协作者，你可以直接推送代码到主仓库：

```bash
# 克隆仓库
git clone https://github.com/Yangtian-yu/1611sys.git

# 创建功能分支
git checkout -b feature/your-feature

# 开发并提交
git add .
git commit -m "feat: add your feature"

# 推送到远程
git push origin feature/your-feature

# 在 GitHub 上创建 Pull Request
```

---

### 方式二：Fork + Pull Request（推荐）

**适合**：所有外部贡献者、开源社区成员

这是标准的开源贡献流程：

#### 1. Fork 仓库

在 GitHub 上点击右上角的 "Fork" 按钮，将项目 Fork 到你的账号下。

#### 2. 克隆你的 Fork

```bash
git clone https://github.com/你的用户名/1611sys.git
cd 1611sys

# 添加上游仓库
git remote add upstream https://github.com/Yangtian-yu/1611sys.git
```

#### 3. 创建功能分支

```bash
# 从 main 分支创建新分支
git checkout -b feature/your-feature-name

# 或修复 bug
git checkout -b fix/bug-description
```

#### 4. 开发和测试

```bash
# 编写代码...

# 运行测试确保代码正常
cd backend && pnpm build
cd ../frontend && pnpm build

# 提交代码
git add .
git commit -m "feat: add amazing feature"
```

#### 5. 保持同步

```bash
# 拉取上游最新更改
git fetch upstream
git rebase upstream/main

# 解决冲突（如果有）
# 继续 rebase
git rebase --continue
```

#### 6. 推送到你的 Fork

```bash
git push origin feature/your-feature-name
```

#### 7. 创建 Pull Request

1. 访问你的 Fork 页面
2. 点击 "Pull Request" 按钮
3. 选择：你的分支 → Yangtian-yu/1611sys 的 main 分支
4. 填写 PR 标题和描述
5. 提交 Pull Request

---

## ✅ 代码审核机制

### Pull Request 审核流程

1. **自动检查**
   - CI/CD 自动运行构建和测试
   - 代码格式检查
   - 类型检查

2. **人工审核**
   - 项目维护者会审查你的代码
   - 可能会提出修改建议
   - 你需要根据反馈进行调整

3. **批准合并**
   - 审核通过后，维护者会合并 PR
   - 代码会自动部署到测试/生产环境

### 审核标准

- ✅ 代码符合项目规范
- ✅ 功能完整且正常工作
- ✅ 有必要的注释和文档
- ✅ 通过所有测试
- ✅ 无明显安全隐患

---

## 📝 提交规范

### Commit Message 格式

使用 [Conventional Commits](https://www.conventionalcommits.org/) 规范：

```
<type>(<scope>): <subject>

<body>

<footer>
```

**Type 类型**：

- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `style`: 代码格式（不影响功能）
- `refactor`: 代码重构
- `perf`: 性能优化
- `test`: 测试相关
- `chore`: 构建/工具链更新

**示例**：

```bash
# 新功能
git commit -m "feat(user): 添加用户头像上传功能"

# Bug 修复
git commit -m "fix(auth): 修复 JWT token 过期问题"

# 文档更新
git commit -m "docs: 更新 API 文档"

# 代码重构
git commit -m "refactor(duty): 优化排班算法性能"
```

### 分支命名规范

- `feature/功能名称` - 新功能开发
- `fix/问题描述` - Bug 修复
- `docs/文档更新` - 文档更新
- `refactor/重构内容` - 代码重构

**示例**：

```bash
feature/add-user-avatar
fix/jwt-expiration-issue
docs/update-api-documentation
refactor/duty-algorithm
```

---

## 🎨 代码规范

### TypeScript/JavaScript

- 使用 TypeScript，避免使用 `any`
- 遵循 ESLint 规则
- 使用有意义的变量和函数名
- 复杂逻辑添加注释

```typescript
// ❌ 不好的例子
const a = (x: any) => x + 1;

// ✅ 好的例子
const calculateNextOrderIndex = (currentIndex: number): number => {
  return currentIndex + 1;
};
```

### Vue 组件

- 使用 Composition API（`<script setup>`）
- Props 和 Emits 要定义类型
- 组件命名使用 PascalCase

```vue
<!-- ✅ 好的例子 -->
<script setup lang="ts">
interface Props {
  userId: string;
  userName: string;
}

const props = defineProps<Props>();
const emit = defineEmits<{
  'update:user': [userId: string];
}>();
</script>
```

### NestJS 后端

- 使用依赖注入
- Controller 只处理请求响应
- 业务逻辑放在 Service
- 使用 DTO 进行数据验证

```typescript
// ✅ 好的例子
@Controller('users')
export class UserController {
  constructor(private readonly userService: UserService) {}

  @Get()
  async findAll(): Promise<User[]> {
    return this.userService.findAll();
  }
}
```

---

## 🧪 测试

在提交 PR 前，请确保：

```bash
# 后端编译测试
cd backend
pnpm build

# 前端编译测试
cd ../frontend
pnpm build

# 本地运行测试
# 确保功能正常工作
```

---

## 📋 Pull Request 模板

创建 PR 时，请包含以下信息：

```markdown
## 📝 变更说明
简要描述本次 PR 的目的和内容

## ✨ 新增功能
- 功能 1
- 功能 2

## 🐛 Bug 修复
- 修复问题 1
- 修复问题 2

## 🔧 其他变更
- 重构 xxx
- 更新文档

## ✅ 测试清单
- [ ] 本地编译通过
- [ ] 功能测试通过
- [ ] 无新增警告或错误

## 📸 截图（如适用）
[添加截图展示变更效果]

## 🔗 相关 Issue
Closes #123
```

---

## ❓ 常见问题

### 1. Fork 后如何同步上游更新？

```bash
# 添加上游仓库（只需一次）
git remote add upstream https://github.com/Yangtian-yu/1611sys.git

# 拉取上游更新
git fetch upstream

# 合并到本地 main
git checkout main
git merge upstream/main

# 推送到你的 Fork
git push origin main
```

### 2. 提交后发现有错误怎么办？

```bash
# 修改最后一次提交
git add .
git commit --amend

# 强制推送（注意：只适用于自己的分支）
git push origin feature/your-feature -f
```

### 3. Pull Request 被要求修改怎么办？

```bash
# 在原分支上继续修改
git checkout feature/your-feature

# 修改代码...
git add .
git commit -m "fix: 根据反馈修改"

# 推送更新
git push origin feature/your-feature

# PR 会自动更新
```

### 4. 如何解决合并冲突？

```bash
# 拉取最新代码
git fetch upstream
git rebase upstream/main

# 手动解决冲突
# 编辑冲突文件，移除冲突标记

# 继续 rebase
git add .
git rebase --continue

# 推送（需要强制推送）
git push origin feature/your-feature -f
```

---

## 🎯 贡献建议

### 适合新手的任务

- 📝 改进文档和注释
- 🐛 修复简单的 Bug
- 🎨 优化 UI 样式
- 🌐 添加国际化支持

### 进阶任务

- ✨ 开发新功能
- ⚡ 性能优化
- 🔒 安全加固
- 🧪 添加测试

---

## 📞 获取帮助

如有问题，可以通过以下方式获取帮助：

- 💬 在 Pull Request 中评论
- 📝 创建 GitHub Issue
- 📧 联系项目维护者

---

## 📄 行为准则

- 尊重所有贡献者
- 保持友善和建设性的讨论
- 接受建设性批评
- 关注项目的最佳利益

---

## 🎉 感谢

感谢所有为项目做出贡献的开发者！你们的每一次贡献都让项目变得更好。

---

**Happy Coding! 🚀**
